Small and lightweight Kalman filter with python interface. Utilizes Eigen's fast linear algebra arithmetrics.

Requires Eigen3, boost.

Installation:
    cd src
    mkdir build
    cd build
    cmake -DCMAKE_INSTALL_PREFIX=<install location> ..
    make install
